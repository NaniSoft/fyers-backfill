# Shell scripts must keep LF line endings on every platform — CRLF breaks the
# shebang (`/usr/bin/env bash\r` -> "bad interpreter: No such file or directory").
*.sh         text eol=lf
install.sh   text eol=lf